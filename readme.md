Built during: http://vanhacks.com/

The application displays free resources for the underprivileged.

Run Instructions:
yarn run dev

The backend flow is in the AWS:
API Gateway -> Lambda -> dynamoDB 

Copies of lambdas are stored in:
backend/*
